# Local development needs no environment variables or Cloudflare credentials.
# For manual production configuration, export D1_DATABASE_ID in your shell.
# See docs/deployment.md for GitHub Actions variables and secrets.

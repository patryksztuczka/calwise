// Vite PWA requires a worker source; this app only uses its manifest.
// Never registered. No fetch handlers, caches, offline fallback, or reload behavior.
// oxlint-disable-next-line unicorn/require-module-specifiers -- Keep this intentionally empty worker a module.
export {};
